_schema-version: "3.2"
ID: rosterplanningvk
version: 1.0.0
modules:
  - name: rosterplanningvk-destination-content
    type: com.sap.application.content
    requires:
      - name: rosterplanningvk-destination-service
        parameters:
          content-target: true
      - name: rosterplanningvk_html_repo_host
        parameters:
          service-key:
            name: rosterplanningvk_html_repo_host-key
      - name: uaa_rosterplanningvk
        parameters:
          service-key:
            name: uaa_rosterplanningvk-key
    parameters:
      content:
        instance:
          destinations:
            - Name: rosterplanningvk_rosterplanningvk_html_repo_host
              ServiceInstanceName: rosterplanningvk-html5-app-host-service
              ServiceKeyName: rosterplanningvk_html_repo_host-key
              sap.cloud.service: rosterplanningvk
            - Authentication: OAuth2UserTokenExchange
              Name: rosterplanningvk_uaa_rosterplanningvk
              ServiceInstanceName: rosterplanningvk-xsuaa-service
              ServiceKeyName: uaa_rosterplanningvk-key
              sap.cloud.service: rosterplanningvk
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true
  - name: rosterplanningvk-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: rosterplanningvk_html_repo_host
        parameters:
          content-target: true
          memory: 1024M
          disk-quota: 1G
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - rosterplanningvk.zip
          name: rosterplanningvk
          target-path: resources/

  - name: rosterplanningvk
    type: html5
    path: .
    build-parameters:
      build-result: .
      # builder: custom
      # commands:
      # - npm install
      # - npm run build
      # - npm run build:cf
      supported-platforms: []

resources:
  - name: rosterplanningvk-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: rosterplanningvk-destination-service
      service-plan: lite
  - name: rosterplanningvk_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: rosterplanningvk-html5-app-host-service
      service-plan: app-host
  - name: uaa_rosterplanningvk
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: rosterplanningvk-xsuaa-service
      service-plan: application
parameters:
  deploy_mode: html5-repo